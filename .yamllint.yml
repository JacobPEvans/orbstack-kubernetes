---
extends: default

ignore: |
  .github/aw/
  .github/workflows/*.lock.yml
  .github/workflows/agentics-maintenance.yml
  secrets.enc.yaml

rules:
  line-length:
    max: 160
  truthy:
    check-keys: false
