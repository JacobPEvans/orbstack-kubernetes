apiVersion: v1
kind: Service
metadata:
  name: cribl-mcp-server
  namespace: monitoring
  labels:
    app: cribl-mcp-server
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: cribl-mcp-server
  ports:
    - name: mcp
      port: 3030
      targetPort: 3030
      protocol: TCP
