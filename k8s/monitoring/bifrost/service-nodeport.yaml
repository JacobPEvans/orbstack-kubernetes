apiVersion: v1
kind: Service
metadata:
  name: bifrost-nodeport
  namespace: monitoring
  labels:
    app: bifrost
spec:
  type: NodePort
  selector:
    app: bifrost
  ports:
    - name: api
      port: 8080
      targetPort: 8080
      nodePort: 30080
      protocol: TCP
